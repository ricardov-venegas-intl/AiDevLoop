using Xunit;

namespace AiDevLoop.Core.Tests;

/// <summary>
/// Basic smoke tests to verify the Core project compiles and test discovery works.
/// </summary>
public class SmokeTests
{
    /// <summary>
    /// Verifies the test project executes — placeholder test.
    /// </summary>
    [Fact]
    public void Project_builds_and_tests_run()
    {
        // simple smoke assertion to verify test project executes
        Assert.True(true);
    }
}
