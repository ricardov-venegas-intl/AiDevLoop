namespace AiDevLoop.Cli;

/// <summary>
/// Minimal CLI entry point (placeholder). Full CLI wiring is implemented in later tasks.
/// </summary>
public static class Program
{
    /// <summary>
    /// Application entry point — returns 0 for a successful no-op placeholder.
    /// </summary>
    /// <param name="args">Command-line arguments (unused).</param>
    public static int Main(string[] args)
    {
        // no-op entry so project compiles and can be validated by CI
        return 0;
    }
}
